torch>=2.0
transformers>=4.40
safetensors
fastapi
uvicorn[standard]
sse-starlette
flashinfer-python>=0.6
# Optional: flash-attn>=2.5 (requires SM>=80, fallback when flashinfer unavailable)
# pip install flash-attn --no-build-isolation
